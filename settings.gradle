pluginManagement {
    repositories {
        maven { url './offline-repository/' }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        maven { url './offline-repository/' }
        mavenCentral()

    }
}

rootProject.name = 'LEDSuite'